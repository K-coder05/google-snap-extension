{
  "name": "snapfit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Choose whether fullscreen fills your whole display or just your split-screen browser window.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "lint": "eslint src test",
    "zip": "npm run build && zip -r snapfit.zip manifest.json dist icons src/popup src/styles"
  },
  "devDependencies": {
    "esbuild": "^0.23",
    "eslint": "^9",
    "jsdom": "^29.1.1",
    "playwright": "^1.47",
    "vitest": "^2"
  }
}
